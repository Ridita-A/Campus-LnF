
  # Campus LnF

  A campus lost and found system for CSE 4508: RDBMS lab.

  ## Running the code

  Run `npm install` to install the dependencies.

  Run `npm run dev` to start the development server, then ctrl+click on the local host link.
  